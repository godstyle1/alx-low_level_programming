#include "main.h"

/**
 * _isupper - This function checks for uppercase character
 * @c: the integer parameter used by the function to make decision
 * Return: 1 if uppercase else 0
 */
int _isupper(int c)
{
	int uppercase = 1;

	if (c == 'A')
	{
		return (uppercase);
	}
	else
	return (0);
}
