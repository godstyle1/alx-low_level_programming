#include "main.h"
/**
 * _isalpha - The alphabet character entry point
 *
 * @c: character parameter for the argument
 *
 * Return: Always 0 (Success)
 */
int _isalpha(int c)
{
	if (c >= 'a' && c <= 'z')
	{
		return (1);
	} else if (c >= 'A' && c <= 'Z')
	{
		return(1)
	}else 
	{ 
		return (0) 
	}
}
