#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 *
 */
int main(void)
{
	int a;
	int j = '9';

	for (a = '0'; a <= j; a++)
	{
		putchar(a);
	}
	putchar('\n');
	return (0);
}
